import { FormInputDemo } from "@/components/examples/form-input-demo";
import { formInputPropsDocs, inputChangePayloadDocs } from "./props-docs";

export const metadata = {
  title: "FormInput - NX Components",
  description:
    "支持受控值、非受控默认值、前后缀、最大长度、清空按钮和校验状态的基础输入框组件。",
};

# FormInput

<div className="mt-4 rounded-2xl border bg-gradient-to-br from-slate-50 to-white p-6 shadow-sm">
  <div className="inline-flex items-center px-0 py-1.5 text-base font-bold tracking-wide text-slate-950 font-semibold text-xl">
    表单组件
  </div>
  <div className="mt-4 max-w-2xl text-base leading-7 text-muted-foreground">
    支持受控值、非受控默认值、前缀节点、后缀节点、最大输入长度、清空按钮、回车事件和校验状态的基础输入框组件。
  </div>
</div>

## 组件预览

<div className="mt-6 rounded-2xl border bg-white p-10 shadow-sm">
  <FormInputDemo />
</div>

## 基础用法

```tsx
import { FormInput } from "@/src/components/form-input";

export function Demo() {
  return (
    <FormInput
      placeholder="请输入内容"
      onChange={({ value, event }) => console.log(value, event)}
      onChangeValue={(value) => console.log(value)}
      onPressEnter={(event) => console.log(event.currentTarget.value)}
    />
  );
}
```

## Props

<div className="grid gap-3">
  {formInputPropsDocs.map((prop) => (
    <div
      key={prop.name}
      className="grid gap-3 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm md:grid-cols-[180px_minmax(0,1fr)_120px] md:items-start"
    >
      <div>
        <div className="font-mono text-sm font-semibold text-sky-700">
          字段名：{prop.name}
        </div>
        <div className="mt-1 text-xs leading-5 text-slate-500">
          描述：{prop.description}
        </div>
      </div>
      <code className="w-fit max-w-full overflow-x-auto whitespace-nowrap rounded-md bg-violet-50 px-2 py-1 text-sm text-violet-700">
        类型：{prop.type}
      </code>
      <div className="text-sm leading-6 text-slate-500">
        默认值：{prop.defaultValue}
      </div>
    </div>
  ))}
</div>

## InputChangePayload

<div className="grid gap-3">
  {inputChangePayloadDocs.map((prop) => (
    <div
      key={prop.name}
      className="grid gap-3 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm md:grid-cols-[180px_minmax(0,1fr)] md:items-start"
    >
      <div>
        <div className="font-mono text-sm font-semibold text-sky-700">
          {prop.name}
        </div>
        <div className="mt-1 text-xs leading-5 text-slate-500">
          {prop.description}
        </div>
      </div>
      <code className="w-fit max-w-full overflow-x-auto whitespace-nowrap rounded-md bg-violet-50 px-2 py-1 text-sm text-violet-700">
        {prop.type}
      </code>
    </div>
  ))}
</div>
