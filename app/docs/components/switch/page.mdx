import { SwitchDemo } from "@/components/examples/switch-demo";

export const metadata = {
  title: "Switch - NX Components",
  description: "支持受控、非受控、加载态、禁用、尺寸和内联文案的开关切换组件。",
};

# Switch

<div className="mt-4 rounded-2xl border bg-gradient-to-br from-slate-50 to-white p-6 shadow-sm">
  <div className="inline-flex rounded-full bg-slate-100 px-3 py-1 text-xs font-medium text-slate-600">
    表单组件
  </div>
  <div className="mt-4 max-w-2xl text-base leading-7 text-muted-foreground">
    支持受控与非受控两种使用方式，内置加载态、禁用态、小尺寸和内联文案，适合用于功能开关、配置项切换和权限控制场景。
  </div>
</div>

## 组件预览

<div className="mt-6 rounded-2xl border bg-white p-10 shadow-sm">
  <SwitchDemo />
</div>

## 基础用法

```tsx
import { Switch } from "@/src/components/switch";

export function Demo() {
  const [checked, setChecked] = React.useState(false);

  return (
    <Switch value={checked} onChangeValue={setChecked} />
  );
}
```

## Props

<div className="grid gap-3">
  <div className="grid gap-2 rounded-2xl border bg-white p-4 shadow-sm md:grid-cols-[180px_1fr_120px]">
    <div>
      <div className="font-mono text-sm font-semibold text-slate-950">value</div>
      <div className="mt-1 text-xs text-muted-foreground">受控值</div>
    </div>
    <code className="w-fit rounded-md bg-slate-100 px-2 py-1 text-sm text-slate-700">boolean</code>
    <div className="text-sm text-muted-foreground">默认值：-</div>
  </div>

  <div className="grid gap-2 rounded-2xl border bg-white p-4 shadow-sm md:grid-cols-[180px_1fr_120px]">
    <div>
      <div className="font-mono text-sm font-semibold text-slate-950">defaultValue</div>
      <div className="mt-1 text-xs text-muted-foreground">非受控默认值</div>
    </div>
    <code className="w-fit rounded-md bg-slate-100 px-2 py-1 text-sm text-slate-700">boolean</code>
    <div className="text-sm text-muted-foreground">默认值：false</div>
  </div>

  <div className="grid gap-2 rounded-2xl border bg-white p-4 shadow-sm md:grid-cols-[180px_1fr_120px]">
    <div>
      <div className="font-mono text-sm font-semibold text-slate-950">disabled</div>
      <div className="mt-1 text-xs text-muted-foreground">是否禁用</div>
    </div>
    <code className="w-fit rounded-md bg-slate-100 px-2 py-1 text-sm text-slate-700">boolean</code>
    <div className="text-sm text-muted-foreground">默认值：false</div>
  </div>

  <div className="grid gap-2 rounded-2xl border bg-white p-4 shadow-sm md:grid-cols-[180px_1fr_120px]">
    <div>
      <div className="font-mono text-sm font-semibold text-slate-950">loading</div>
      <div className="mt-1 text-xs text-muted-foreground">是否显示加载状态</div>
    </div>
    <code className="w-fit rounded-md bg-slate-100 px-2 py-1 text-sm text-slate-700">boolean</code>
    <div className="text-sm text-muted-foreground">默认值：false</div>
  </div>

  <div className="grid gap-2 rounded-2xl border bg-white p-4 shadow-sm md:grid-cols-[180px_1fr_120px]">
    <div>
      <div className="font-mono text-sm font-semibold text-slate-950">size</div>
      <div className="mt-1 text-xs text-muted-foreground">尺寸</div>
    </div>
    <code className="w-fit rounded-md bg-slate-100 px-2 py-1 text-sm text-slate-700">"default" | "small"</code>
    <div className="text-sm text-muted-foreground">默认值：default</div>
  </div>

  <div className="grid gap-2 rounded-2xl border bg-white p-4 shadow-sm md:grid-cols-[180px_1fr_120px]">
    <div>
      <div className="font-mono text-sm font-semibold text-slate-950">checkedChildren</div>
      <div className="mt-1 text-xs text-muted-foreground">选中时显示的内联内容</div>
    </div>
    <code className="w-fit rounded-md bg-slate-100 px-2 py-1 text-sm text-slate-700">ReactNode</code>
    <div className="text-sm text-muted-foreground">默认值：-</div>
  </div>

  <div className="grid gap-2 rounded-2xl border bg-white p-4 shadow-sm md:grid-cols-[180px_1fr_120px]">
    <div>
      <div className="font-mono text-sm font-semibold text-slate-950">unCheckedChildren</div>
      <div className="mt-1 text-xs text-muted-foreground">未选中时显示的内联内容</div>
    </div>
    <code className="w-fit rounded-md bg-slate-100 px-2 py-1 text-sm text-slate-700">ReactNode</code>
    <div className="text-sm text-muted-foreground">默认值：-</div>
  </div>

  <div className="grid gap-2 rounded-2xl border bg-white p-4 shadow-sm md:grid-cols-[180px_1fr_120px]">
    <div>
      <div className="font-mono text-sm font-semibold text-slate-950">onChange</div>
      <div className="mt-1 text-xs text-muted-foreground">值变化时触发</div>
    </div>
    <code className="w-fit max-w-full overflow-x-auto rounded-md bg-slate-100 px-2 py-1 text-sm text-slate-700">(payload: SwitchChangePayload) => void</code>
    <div className="text-sm text-muted-foreground">默认值：-</div>
  </div>

  <div className="grid gap-2 rounded-2xl border bg-white p-4 shadow-sm md:grid-cols-[180px_1fr_120px]">
    <div>
      <div className="font-mono text-sm font-semibold text-slate-950">onChangeValue</div>
      <div className="mt-1 text-xs text-muted-foreground">值变化时触发，仅返回当前值</div>
    </div>
    <code className="w-fit max-w-full overflow-x-auto rounded-md bg-slate-100 px-2 py-1 text-sm text-slate-700">(value: boolean) => void</code>
    <div className="text-sm text-muted-foreground">默认值：-</div>
  </div>
</div>
